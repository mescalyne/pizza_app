import 'package:flutter/material.dart';

class PickUpMobileWebPage extends StatelessWidget {
  const PickUpMobileWebPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}