import 'package:flutter/material.dart';

class PickUpWebPage extends StatelessWidget {
  const PickUpWebPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}