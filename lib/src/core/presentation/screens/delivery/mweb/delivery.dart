import 'package:flutter/material.dart';

class DeliveryMobileWebPage extends StatelessWidget {
  const DeliveryMobileWebPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}