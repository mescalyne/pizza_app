import 'package:flutter/material.dart';

class DeliveryWebPage extends StatelessWidget {
  const DeliveryWebPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}